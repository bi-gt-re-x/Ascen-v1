/**
 * Charts, drawn as inline SVG rather than through a library.
 *
 * See LineChart for why. More shapes join it as the growth and analytics pages
 * need them — a bar chart for daily XP, the pentagon for the report card.
 */
export { LineChart } from './LineChart';
export type { LineChartProps } from './LineChart';
